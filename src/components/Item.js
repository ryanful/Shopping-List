import React from "react";
import { StyleSheet, Text, View } from "react-native";

function Item(props) {
  return (
    <View>
      <Text> Item </Text>
    </View>
  );
}

const styles = StyleSheet.create({});

export default Item;
